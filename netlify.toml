# Netlify configuration for Git-connected (continuous) deploys.
[build]
  command = "npx vite build"
  publish = "dist"
  functions = "netlify/functions"

# SPA fallback so deep links / refresh work on any route.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
